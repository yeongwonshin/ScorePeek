# SQLite database path. The database file is created automatically.
SCOREBOARD_DB_PATH=data/scorepeek.sqlite

# Change this in production. It is used to hash anonymous participant keys.
SCOREBOARD_SECRET_SALT=replace-with-random-production-salt
